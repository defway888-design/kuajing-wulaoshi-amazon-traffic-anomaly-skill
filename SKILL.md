---
name: kuajing-wulaoshi-amazon-traffic-anomaly
description: 跨境吴老师亚马逊运营专属父商品流量异动分析 Skill。用于用户要求分析 Amazon 父 ASIN/父商品的流量上涨、流量下降、Sessions/Page Views 异动、广告花费变化、断货到货、Deals、站外推广、变体变化、Listing 状态、自然关键词排名、自然关键词数量、主要关键词搜索趋势、联盟客推广等流量原因时使用；同时交付运营可读报告和同源结构化诊断证据，供上游周数据分析生成行动建议；只处理流量异动，不处理转化率/CVR 异动，本 Skill 不输出优化建议。
---

# 跨境吴老师亚马逊父商品流量异动分析

## 使用边界

本 Skill 为跨境吴老师专用模板，未经授权不得移除、替换或弱化 Skill 名称、执行提示和页面标题中的跨境吴老师标识。

只分析父商品维度的流量异动。不要做子 ASIN 单独归因，不要做转化率异动分析，不要输出优化建议。

当用户输入子 ASIN 时，必须先通过领星 MCP 将它归属到父商品 ASIN。父商品口径只以领星 MCP 为准，不使用卖家精灵、SIF 或互联网结果推断父子关系。

如果同一 ASIN 在多个市场存在，且用户没有指定市场，必须让用户选择具体市场或明确选择“全部市场”。选择全部市场时，按市场分别运行和输出，不要跨市场合并。

## 必读参考

执行正式分析前必须读取：

- `references/traffic-anomaly-playbook.md`：业务口径、因素清单、输出规则和禁止残留逻辑。
- `references/fixed-mcp-runbook.md`：每一步固定调用的 MCP、工具/数据库、参数、字段和阻塞条件。
- `references/structured-evidence-contract.md`：与周数据分析主 Skill 对接的结构化证据字段、状态和输出约束。

不得只凭 `SKILL.md` 执行正式分析。

不要要求使用者把本 Skill 安装到固定本机路径。所有引用本 Skill 内资源的地方都必须使用当前已安装 Skill 的相对资源路径，不得写入 Windows 盘符路径、用户名目录或开发者机器路径。MCP 名称按数据源身份固定为领星 MCP、卖家精灵 MCP、SIF MCP，但实际工具前缀必须在当前运行人员自己的 Codex/MCP 环境中解析，不得硬编码开发环境的工具别名。

## 品牌执行提示

- 开始多步骤诊断时输出：`跨境吴老师正在准备父商品流量异动诊断...`
- 获取父商品、指标或证据时按真实状态输出：`跨境吴老师正在获取流量诊断证据...`
- 生成报告和结构化结果时输出：`跨境吴老师正在生成流量异动诊断结果...`
- 等待补充输入时使用：`跨境吴老师需要补充<具体信息>。`
- 核心条件阻断时使用：`跨境吴老师当前无法继续流量异动诊断：<事实原因>。`
- 成功完成时使用：`跨境吴老师流量异动诊断已完成。`

提示必须反映真实执行状态，不得在数据尚未取得或结果尚未生成时提前宣告完成。

## 执行流程

1. 确认输入：ASIN、市场、流量方向或上游周数据分析 Skill 给出的流量异动结果。上游调用时必须原样接收 `记录键`、父 ASIN、站点、流量方向、当前比较窗口、基准比较窗口及两期流量值；独立运行时未提供 `记录键`，结构化结果写 `未提供`，不得自行拼接成上游正式记录键。分析月份不是必填项；用户未指定时间时，默认按当前月已发生区间运行。
2. 用领星 MCP 标准化父商品口径：先在当前用户自己的领星 MCP 环境中动态获取店铺、市场、`sid`、广告 `profile_id` 等运行参数，再用本次用户输入的 ASIN 和领星返回的父 ASIN、子 ASIN、SKU 生成后续 `search_value`，确定父 ASIN、市场、父商品下分析期子体与对比期子体并集。不得使用开发测试环境中的固定 `sid`、店铺名、`profile_id`、测试 ASIN、测试 SKU 或其他账号参数。
3. 建立时间窗口：
   - 用户未指定时间：默认当前月 1 日至当前日期，并对比上月 1 日起的同等天数；例如 7 月 8 日运行时，默认对比 7 月 1 日至 7 月 8 日 vs 6 月 1 日至 6 月 8 日。
   - 如果数据源实际只能取到更早日期，以实际调取到的最新日期为准；例如 7 月 8 日运行但数据只到 7 月 6 日，则对比 7 月 1 日至 7 月 6 日 vs 6 月 1 日至 6 月 6 日。
   - 用户只指定一个月份或月份段，且没有指定对比时间段时，默认用该时间段 vs 前一个等长时间段；例如 2026 年 6 月 vs 2026 年 5 月，2026 年 4-6 月 vs 2026 年 1-3 月。
   - 用户明确指定两个时间段时，严格按用户指定的两个时间段对比，不强制使用上月或同期；例如 2026 年 1-3 月 vs 2026 年 4-6 月，或 2026 年 2 月 vs 2026 年 6 月。
   - 用户未说明哪段是分析期时，默认较晚时间段为分析期，较早时间段为对比期；用户明确指定“用 A 对比 B”时，按用户语义确定分析期和对比期。
   - 两个自定义时间段天数不一致时，最终证据必须同时输出总量和日均值；除非用户明确要求比较总量，否则流量方向和因素方向优先按日均值判断。
   - SIF 自然搜索数据无法自定义日期时，优先用 month；当前月未结束时优先用 SIF 最新完整周与上月同口径周对比，周口径不可用时再用 latelyDay=7/30 或 lately=7/30 兜底。
4. 严格按 `references/fixed-mcp-runbook.md` 调用指定 MCP、工具/数据库、参数和字段，完成所有验证项。
5. 对卖家精灵高频接口必须先做调用计划，按流量方向排除不可能因素，串行调用、缓存去重、命中即停止；自然搜索三项只允许调用 SIF MCP，不得退回卖家精灵流量词、关键词反查或关联流量接口。
6. 按 `references/structured-evidence-contract.md` 区分核心诊断条件与辅助验证项。核心条件全部通过后，即使部分辅助项缺失，也要保留已验证结论并标记 `可用但有证据缺口`。
7. 只有父 ASIN/站点、流量方向、当前与基准窗口、两期核心流量值、等天数或已完成日均归一化、核心来源一致性、可复核诊断结论中的任一项无法确认时，才标记 `诊断失败` 并停止生成正式因素结论。Seller ID、站外推广、联盟客推广、部分 Listing 辅助项等缺失不得单独造成整份诊断失败。
8. 不允许用其他 MCP 或工具替代指定 MCP。例如：领星父商品关系不能用卖家精灵或 SIF 代替；卖家精灵 Deals 不能用手工 Keepa 文件代替；SIF 自然搜索数据不能用 Google Trends 或卖家精灵代替。
9. 结果输出时只保留与流量方向一致且有数据证据的可能因素；不区分可控因素和不可控因素，不展示未命中因素，不给优化建议。
10. 最终输出的数据证据必须写成运营可读的自然语言，并包含具体数值、时间窗口和变化量。不得输出 MCP 字段名、字段表达式、等号判断或“分析期低于对比期”这类泛泛描述。
11. 每次诊断必须从同一个内部证据对象同时生成运营可读报告和结构化证据。不得先生成 Word/文字报告后再反向解析，不得让另一个模型重新概括后生成结构化结果。
12. 结构化证据必须严格使用 `references/structured-evidence-contract.md` 的 12 个字段。流量诊断只输出流量因素，不生成可控制因素、可执行动作、P0、P1或任何运营行动建议。
13. 上游周数据分析 Skill 调用时，返回诊断总状态、证据缺口清单和结构化证据行；单个辅助验证项失败只影响该项，不得抹掉其他已完成证据。

## 最终输出格式

先输出诊断总状态：

```text
核心诊断状态：完整完成 / 可用但有证据缺口 / 诊断失败
证据缺口：无 / <缺口类型与说明>
```

`完整完成` 和 `可用但有证据缺口` 继续输出以下运营可读结果：

```text
流量异动方向：流量增加/流量减少

可能的因素：

1. 因素名称
   数据来源：领星 MCP/卖家精灵 MCP/SIF MCP/互联网搜索
   数据证据：用具体时间窗口、对比期值、分析期值、变化值、变化率、子体数量或已确认数量下限、异常示例、日期或链接描述证据；只写业务语言，不写底层字段名。
   可能原因：只说明该因素为什么可能造成当前流量方向。
```

如果没有任何因素命中，输出：

```text
流量异动方向：流量增加/流量减少

可能的因素：无
```

如果核心诊断失败，禁止输出上面的结果格式，改为输出：

```text
核心验证未完成，无法生成流量异动结果。

未完成验证项：
1. 验证项名称
   指定数据来源：MCP + 工具/数据库
   缺失内容：字段/权限/时间窗口/网页证据
   处理要求：补齐核心条件后重新运行诊断
```

最后按 `references/structured-evidence-contract.md` 返回同源结构化证据。`诊断失败` 时结构化证据行为空，但必须返回诊断总状态和核心阻断原因。
