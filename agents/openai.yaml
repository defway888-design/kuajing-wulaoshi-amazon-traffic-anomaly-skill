interface:
  display_name: "跨境吴老师父商品流量异动"
  short_description: "诊断父商品流量异动并输出同源结构化证据。"
  default_prompt: "Use $kuajing-wulaoshi-amazon-traffic-anomaly to diagnose an Amazon parent ASIN traffic change and return same-run structured evidence without conversion-rate or action advice."

policy:
  allow_implicit_invocation: true
